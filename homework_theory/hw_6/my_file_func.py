# 1
# Модуль.
# Перенесите ваши функции из прошлого домашнего задания в отдельный
# файл и импортируйте их в основной (исполняемый) файл.
# Запустите каждую вашу функцию по 1 или более раз в исполняемом файле

#из этого файла перенесла функции в hw_perenos

# def sum_ignore_non_numbers(items):
#     total = 0
#     for i in items:
#         if isinstance (i, (int, float)):
#             total += i
#     return total
#
#
# def is_triangle(a, b, c):
#     if a + b > c and a + c > b and b + c > a:
#         return True
#     else:
#         return False

#Домашка с ревью - будем переносить функции в HW_perenos:
# def mul(a, b):
#     return a*b
#
# def div(c, d):
#     if d == 0:
#         return "Not zero"
#     return c/d
#
# def summ(e, f):
#     return e+f
#
# def sub(g, h):
#     return g-h